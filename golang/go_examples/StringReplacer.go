package main

/*
import (
	"fmt"
	"strings"
)

// Below code will replace characters in the given string
// with the given character
// Uncomment the below code and then run

func main() {
	brokenString := "He##o World"
	replacerString := strings.NewReplacer("#", "l")
	fixedString := replacerString.Replace(brokenString)

	fmt.Println("Fixed String : ", fixedString)
}
*/
