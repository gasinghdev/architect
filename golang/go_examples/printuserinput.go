package main

/*
import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	fmt.Print("Please enter something : ")
	reader := bufio.NewReader(os.Stdin)
	input, error := reader.ReadString('\n')
	if error != nil {
		fmt.Println("Error Ocurred", error)
	}
	fmt.Println("you typed : ", input)
}
*/
